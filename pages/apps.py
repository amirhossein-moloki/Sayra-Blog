from django.apps import AppConfig


class PagesConfig(AppConfig):
    """
    EN: Application configuration for the 'pages' app.
    FA: تنظیمات اپلیکیشن برای بخش صفحات ('pages').
    """

    default_auto_field = "django.db.models.BigAutoField"
    name = "pages"
