# EN: Core models are typically defined in base_models.py to avoid circular imports.
# FA: مدل‌های اصلی معمولاً در فایل base_models.py تعریف می‌شوند تا از وارد کردن چرخه‌ای (circular import) جلوگیری شود.
