from django.apps import AppConfig


class InteractionsConfig(AppConfig):
    """
    EN: Application configuration for the 'interactions' app.
    FA: تنظیمات اپلیکیشن برای بخش تعاملات ('interactions').
    """

    default_auto_field = "django.db.models.BigAutoField"
    name = "interactions"
